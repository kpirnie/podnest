[Definition]
; matches the proxy access log format:
; 2026-05-06T14:23:01Z GET host /path STATUS bytes duration IP "UA"
;
; we trigger on 4xx status codes (400-499) logged by the proxy
failregex = ^\S+ \S+ \S+ \S+ 4\d\d \S+ \S+ <HOST> .*$

ignoreregex =